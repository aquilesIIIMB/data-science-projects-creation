{
  "": {
    "numberOfColumns": 3,
    "fields": [
      {
        "key": "projectName",
        "name": "Project Name",
        "type": "STRING",
        "required": true,
        "validationRules": [
          {
            "min": 3
          },
          {
            "max": 30
          },
          {
            "pattern": "^[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]$",
            "message": "Must contain only lowercase letters, numbers, and the '-' (dash) character (Cannot start or end with a dash)"
          }
        ]
      },
      {
        "key": "description",
        "name": "Description",
        "type": "TEXT_AREA",
        "required": false,
        "colSpan": 3,
        "validationRules": [
          {
            "type": "string",
            "max": 250,
            "message": "Maximum length is 250 characters"
          }
        ]
      },
      {
        "key": "applicationName",
        "name": "Application Name",
        "type": "STRING",
        "required": true,
        "validationRules": [
          {
            "min": 3
          },
          {
            "max": 20
          },
          {
            "pattern": "^[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]$",
            "message": "Must contain only lowercase letters, numbers, and the '-' (dash) character (Cannot start or end with a dash)"
          }
        ]
      }
    ]
  }
}
